export const FIREBASE_CONFIG = {
    apiKey: build.env.FIREBASE.API_KEY,
    authDomain: build.env.FIREBASE.AUTH_DOMAIN,
    databaseURL: build.env.FIREBASE.DATABSE_URL,
    projectId: build.env.FIREBASE.PROJECT_ID,
    storageBucket: build.env.FIREBASE.STORAGE_BUCKET,
    messagingSenderId: build.env.FIREBASE.MESSAGING_SENDER_ID,
};
